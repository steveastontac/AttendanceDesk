<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".ui.dashboard.statistics"></activity>
        <activity android:name=".ui.notifications.support" />
        <activity android:name=".ui.notifications.about" />
        <activity android:name=".ui.home.fourthyear" />
        <activity android:name=".ui.home.thirdyear" />
        <activity android:name=".ui.home.secondyear" />
        <activity android:name=".ui.home.firstyear" />
        <activity
            android:name=".screen2"
            android:label="@string/title_activity_screen2" />
        <activity android:name=".DashActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SecondaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>